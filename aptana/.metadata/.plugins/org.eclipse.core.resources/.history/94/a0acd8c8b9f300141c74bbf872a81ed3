(function() {
	angular.module('main')
	.controller('InputTable', ['$scope', 'tableService', function($scope, tableService) {
		$scope.$on('handleBroadcast', function() {
			alert("Broadcast with ID " + tableService.getTab());
			$scope.data.push();
		});
		
		$scope.data = [];

	}]);
})();
