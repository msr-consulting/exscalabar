(function() {
	angular.module('main')
	.controller('Save', ['$scope', 'SaveData', function($scope, SaveData) {

		$scope.save = function() {
			var xml = '<?xml version="1.0" encoding="utf-8"?>\r\n<OZONE>\r\n';
			SaveData.getData().forEach(function(entry) {
				xml += "\t<" + entry.id + ">" + entry.val + '<\\' + entry.id + '>\r\n';
			});
			alert(xml);
		};
	}]);
})(); 